// lib/utils/colors.dart
import 'package:flutter/material.dart';

// Defining the color palette inspired by the Vyana UI mockups.
// This ensures brand consistency across the app.

const Color primaryColor = Color(0xFF00A896); // A rich, calming cyan/teal
const Color secondaryColor = Color(0xFFF5F5DC); // A soft, creamy beige for backgrounds
const Color accentColor = Color(0xFFFF9F1C); // A warm, inviting amber for accents
const Color textColor = Color(0xFF2C3E50); // A dark, elegant slate for text
const Color subtleTextColor = Color(0xFF7F8C8D); // A softer gray for subtitles
const Color errorColor = Color(0xFFE74C3C); // A clear, noticeable red for errors